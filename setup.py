#!/usr/bin/env python
from setuptools import setup

setup(
    name="SCMAAR",
    version="0.0.0",
    packages=[
        "scmaar"
    ],
    install_requires=[
    ],
    extras_require={
        "build": []
    }
)
